import { StyleSheet } from 'react-native';

import defaultStyles from '../../global/defaultStyles';

const styles = StyleSheet.create({
  screen: {
    paddingTop: defaultStyles.statusBarHeight,
    flex: 1,
  },
});

export default styles;
